# Changelog

## 0.1.0

- Initial desktop VS Code extension package.
- Validates CSS files with the CSS Property Type Validator core package.
- Supports configured shared `@property` registry file globs.
